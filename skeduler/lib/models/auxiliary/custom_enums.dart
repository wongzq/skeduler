enum MemberOption {
  makeOwner,
  makeAdmin,
  makeMember,
  edit,
  remove,
}

enum SubjectOption {
  edit,
  remove,
}

enum TimetableEditorOption {
  switchAxis,
  addSubject,
  addDummy,
  clearData,
  settings,
  save,
}

enum CustomOption {
  edit,
  reorder,
  remove,
}

enum AvailabilityOption {
  edit,
  remove,
}

